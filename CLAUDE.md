# Eye Candy — Claude Code

See **`AGENTS.md`** in the repo root. It is the agent-agnostic source of truth
for this repo and applies to Claude Code unchanged.

Nothing Claude-specific is maintained here — if a rule needs to change, change
`AGENTS.md` (or the `docs/` file it points at) so every agent picks it up.
